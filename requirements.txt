# Core Data & Analysis
pandas==2.1.3
# Use numpy 1.26.x to satisfy pandas 2.1.x dependency on Python 3.13 environments
numpy==1.26.4
# scipy removed from cloud requirements because building SciPy on
# Streamlit Cloud's Python 3.13 environment requires a Fortran
# compiler (gfortran) and other build tools not available in the
# build image. Install SciPy locally if you need it for backtesting.
# scipy==1.11.4

# Market Data
yfinance==0.2.32

# Brokers
# `ib-insync` is omitted from core requirements for Streamlit deployment
# (execution layer is optional and depends on IBKR; install locally when needed)

# Dashboard & Visualization
streamlit==1.28.1
plotly==5.18.0

# Database
sqlalchemy==2.0.23

# Async
aiohttp==3.9.1

# Utilities
python-dotenv==1.0.0
requests==2.31.0
tqdm==4.66.1
pyyaml==6.0.1

# Development & Testing
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.12.0
flake8==6.1.0
mypy==1.7.0


